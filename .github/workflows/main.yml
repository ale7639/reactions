name: Hello word 

on: 
 push:
   branches: [main]
 workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
       uses: actions/checkout@v3

      - name: say hello 
        run: echo "Hello, GitHub Actions!"
